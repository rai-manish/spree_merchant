require "spec_helper"

describe SpreeMerchant do
  it "has a version number" do
    expect(SpreeMerchant::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
